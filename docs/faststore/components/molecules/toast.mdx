---
title: "Toast"
excerpt: Toasts are used to display notifications. They appear temporarily, and should contain a single line of text directly related to the operation performed.
keywords:
  - UI library
  - ecommerce
  - react
sidebar_custom_props:
  image: https://vtexhelp.vtexassets.com/assets/docs/src/PaymentMethods___b0f344b254360daa53406cd631887adf.png
---

[<i class="fa-brands fa-github"></i> Source code](https://github.com/vtex/faststore/tree/main/packages/components/src/molecules/Toast)

Toasts are used to display notifications. They appear temporarily, and should contain a single line of text directly related to the operation performed.

<Tabs items={['Example', 'Code']} defaultIndex="0">
  <Tab>
    <OverviewSection direction="row">
      <UIProvider>
        <ToastUsage message={'Simple Toast message.'} status={'INFO'} btnLabel="Simple Toast" />
        <ToastUsage title={'Hooray!'} message={'Thank for your Click.'} status={'INFO'} icon={<Icon name="SealCheck" width={30} height={30} />} btnLabel="Info Toast" />
        <ToastUsage title={'Oops.'} message={'Something went wrong. Please Try again.'} status={'ERROR'} icon={<Icon name="SealWarning" width={30} height={30} />} btnLabel="Error Toast" />
        <ToastUsage title={'Warn.'} message={'Warning: Please Try again.'} status={'WARNING'} icon={<Icon name="SealWarning" width={30} height={30} />} btnLabel="Warning Toast" />
      </UIProvider>
    </OverviewSection>
  </Tab>
  <Tab>
    ```tsx
      const Toast = () => {
        const { toasts, pushToast } = useUI()

        return (
          <>
            <Button
              variant="primary"
              onClick={() =>
                pushToast({
                  message: 'Simple Toast message.',
                  status: 'INFO',
                })
              }
              style={{ width: '16rem' }}
            >
              Simple Toast
            </Button>
            {toasts.length > 0 && <Toast />}
          </>
        )
      }
    ```

    ```tsx
      const Toast = () => {
        const { toasts, pushToast } = useUI()

        return (
          <>
            <Button
              variant="primary"
              onClick={() =>
                pushToast({
                  title: 'Hooray!',
                  message: 'Thank for your Click.',
                  status: 'INFO',
                  icon: <Icon name="SealCheck" width={30} height={30} />,
                })
              }
              style={{ width: '16rem' }}
            >
              Info Toast
            </Button>
            {toasts.length > 0 && <Toast />}
          </>
        )
      }
    ```

    ```tsx
      const Toast = () => {
        const { toasts, pushToast } = useUI()

        return (
          <>
            <Button
              variant="primary"
              onClick={() =>
                pushToast({
                  title: 'Oops.',
                  message: 'Something went wrong. Please Try again.',
                  status: 'ERROR',
                  icon: <Icon name="SealWarning" width={30} height={30} />,
                })
              }
              style={{ width: '16rem' }}
            >
              Error Toast
            </Button>
            {toasts.length > 0 && <Toast />}
          </>
        )
      }
    ```

    ```tsx
      const Toast = () => {
        const { toasts, pushToast } = useUI()

        return (
          <>
            <Button
              variant="primary"
              onClick={() =>
                pushToast({
                  title: 'Oops.',
                  message: 'Warn: Please Try again.',
                  status: 'WARNING',
                  icon: <Icon name="SealWarning" width={30} height={30} />,
                })
              }
              style={{ width: '16rem' }}
            >
              Warning Toast
            </Button>
            {toasts.length > 0 && <Toast />}
          </>
        )
      }
    ```

  </Tab>
</Tabs>

---

## Usage

### Import the component

```tsx
import { Toast } from "@faststore/ui";
```

### Import styles

To apply styles, include the following in your stylesheet:

```scss
@import "@faststore/ui/src/components/molecules/Toast/styles.scss";
```

For details, see [Importing FastStore UI component styles](https://developers.vtex.com/docs/guides/faststore/using-themes-importing-ui-components-styles).

---

## Design tokens

<TokenTable>
  <TokenRow
    token="--fs-toast-width"
    value="calc(100% - (2 * var(--fs-spacing-3)))"
  />
  <TokenRow token="--fs-toast-min-height" value="var(--fs-spacing-9)" />
  <TokenRow
    token="--fs-toast-padding"
    value="var(--fs-spacing-1) var(--fs-spacing-3) var(--fs-spacing-1) var(--fs-spacing-1)"
  />
  <TokenRow
    token="--fs-toast-margin"
    value="var(--fs-spacing-3) var(--fs-spacing-3) 0 var(--fs-spacing-3)"
  />
  <TokenDivider />
  <TokenRow token="--fs-toast-top-mobile" value="3.125rem" />
  <TokenRow token="--fs-toast-top-tablet" value="6.25rem" />
  <TokenDivider />
  <TokenRow
    token="--fs-toast-border-radius"
    value="var(--fs-border-radius-medium)"
  />
  <TokenRow token="--fs-toast-border-width" value="var(--fs-border-width)" />
  <TokenRow token="--fs-toast-border-color" value="transparent" isColor />
  <TokenDivider />
  <TokenRow token="--fs-toast-shadow" value="var(--fs-shadow-hover)" />
  <TokenRow
    token="--fs-toast-bkg-color"
    value="var(--fs-color-neutral-0)"
    isColor
  />
  <TokenDivider />
  <TokenRow
    token="--fs-toast-transition-property"
    value="var(--fs-transition-property)"
  />
  <TokenRow
    token="--fs-toast-transition-timing"
    value="var(--fs-transition-timing)"
  />
  <TokenRow
    token="--fs-toast-transition-function"
    value="var(--fs-transition-function)"
  />
</TokenTable>

### Nested Elements

#### Icon

<TokenTable>
  <TokenRow
    token="--fs-toast-icon-container-min-width"
    value="var(--fs-spacing-7)"
  />
  <TokenRow
    token="--fs-toast-icon-container-height"
    value="var(--fs-toast-icon-container-min-width)"
  />
  <TokenRow
    token="--fs-toast-icon-container-bkg-color"
    value="var(--fs-color-primary-bkg-light)"
    isColor
  />
  <TokenRow
    token="--fs-toast-icon-container-border-radius"
    value="var(--fs-border-radius)"
  />
</TokenTable>

#### Title

<TokenTable>
  <TokenRow token="--fs-toast-title-size" value="var(--fs-text-size-body)" />
  <TokenRow
    token="--fs-toast-title-weight"
    value="var(--fs-text-weight-bold)"
  />
  <TokenRow token="--fs-toast-title-line-height" value="1.2" />
  <TokenRow token="--fs-toast-title-margin-left" value="var(--fs-spacing-3)" />
</TokenTable>

#### Message

<TokenTable>
  <TokenRow
    token="--fs-toast-message-size"
    value="var(--fs-toast-title-size)"
  />
  <TokenRow
    token="--fs-toast-message-line-height"
    value="var(--fs-toast-title-line-height)"
  />
  <TokenRow
    token="--fs-toast-message-margin-left"
    value="var(--fs-spacing-3)"
  />
</TokenTable>

---

## Data attributes

You can target and override `Toast` styles using the following data attributes:

`data-fs-toast`

`data-fs-toast-visible="true | false"`

`data-fs-toast-icon-container`

`data-fs-toast-content`

`data-fs-toast-title`

`data-fs-toast-message`

---

## Props

Toast's props are in the object passed as an argument by `pushToast(toast: Props)` method.

<PropsSection propsList={propsToast} />
