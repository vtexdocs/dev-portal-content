---
title: "Table"
excerpt: Displays information in a friendly way, allowing users to scan for details quickly.
sidebar_custom_props:
  image: https://vtexhelp.vtexassets.com/assets/docs/src/Table___06dbb666025a4d64540fb91d22b54ba6.png
components:
  - Table.tsx
  - TableBody.tsx
  - TableHead.tsx
  - TableRow.tsx
  - TableFooter.tsx
  - TableCell.tsx
---

[<i class="fa-brands fa-github"></i> Source code](https://github.com/vtex/faststore/tree/main/packages/components/src/molecules/Table)

Tables display information in a friendly way, allowing users to scan for details quickly.The Table component is a compound of the following:

- `Table` - renders a `<div>` as wrapper with a `<table>` tag inside it.
- `TableBody` - renders a `<tbody>` tag.
- `TableHead` - renders a `<thead>` tag.
- `TableRow` - renders a `<tr>` tag.
- `TableFooter` - renders a `<tfoot>` tag.
- `TableCell` - renders a `<th>` or `<td>` tag depending on the value of the `variant` prop.

<Tabs items={['Example', 'Code']} defaultIndex="0">
  <Tab>
    <OverviewSection
      containerStyle={{ display: 'grid', gridTemplateColumns: '1fr 1fr' }}>
      <Table>
        <TableHead>
          <TableRow>
            <TableCell scope="col" variant="header" align="left">
              Colored + Icon
            </TableCell>
            <TableCell scope="col" variant="header" align="right">
              Values
            </TableCell>
          </TableRow>
        </TableHead>
        <TableBody>
          {sizes.map((size) => (
            <TableRow key={size.type}>
              <TableCell variant="header" align="left">
                <Icon name="Ruler" />
                {size.type}
              </TableCell>
              <TableCell align="right">{size.value}</TableCell>
            </TableRow>
          ))}
        </TableBody>
      </Table>
      <Table variant="bordered">
        <TableHead>
          <TableRow>
            <TableCell scope="col" variant="header" align="left">
              Bordered + Icon
            </TableCell>
            <TableCell scope="col" variant="header" align="right">
              Total
            </TableCell>
          </TableRow>
        </TableHead>
        <TableBody>
          {options.map((option) => (
            <TableRow key={option.numberOfInstallments}>
              <TableCell variant="header" align="left">
                <Icon name="ShoppingCart" />
                {option.numberOfInstallments}x
              </TableCell>
              <TableCell align="right">
                <Price formatter={useFormattedPrice} value={option.total} />
              </TableCell>
            </TableRow>
          ))}
        </TableBody>
      </Table>
    </OverviewSection>
    <OverviewSection containerStyle={{ display: 'grid', gridTemplateColumns: '1fr 1fr' }}>
      <UsageTableColored/>
      <UsageTableBordered/>
    </OverviewSection>
  </Tab>
  <Tab>
```tsx
  // Colored + Icon
  <Table>
    <TableHead>
      <TableRow>
        <TableCell scope="col" variant="header" align="left">
          Colored + Icon
        </TableCell>
        <TableCell scope="col" variant="header" align="right">
          Values
        </TableCell>
      </TableRow>
    </TableHead>
    <TableBody>
      {sizes.map((size) => (
        <TableRow key={size.type}>
          <TableCell variant="header" align="left">
            <Icon name="Ruler" />
            {size.type}
          </TableCell>
          <TableCell align="right">{size.value}</TableCell>
        </TableRow>
      ))}
    </TableBody>
  </Table>

// Bordered + Icon

<Table variant="bordered">
  <TableHead>
    <TableRow>
      <TableCell scope="col" variant="header" align="left">
        Bordered + Icon
      </TableCell>
      <TableCell scope="col" variant="header" align="right">
        Total
      </TableCell>
    </TableRow>
  </TableHead>
  <TableBody>
    {options.map((option) => (
      <TableRow key={option.numberOfInstallments}>
        <TableCell variant="header" align="left">
          <Icon name="ShoppingCart" />
          {option.numberOfInstallments}x
        </TableCell>
        <TableCell align="right">
          <Price formatter={useFormattedPrice} value={option.total} />
        </TableCell>
      </TableRow>
    ))}
  </TableBody>
</Table>

// Colored

<Table>
  <TableHead>
    <TableRow>
      <TableCell scope="col" variant="header" align="left">
        Colored
      </TableCell>
      <TableCell scope="col" variant="header" align="right">
        Total
      </TableCell>
    </TableRow>
  </TableHead>
  <TableBody>
    {options.map((option) => (
      <TableRow key={option.numberOfInstallments}>
        <TableCell variant="header" align="left">
          {option.numberOfInstallments}x
        </TableCell>
        <TableCell align="right">
          <Price formatter={useFormattedPrice} value={option.total} />
        </TableCell>
      </TableRow>
    ))}
  </TableBody>
</Table>

// Bordered

  <Table variant="bordered">
    <TableHead>
      <TableRow>
        <TableCell scope="col" variant="header" align="left">
          Bordered
        </TableCell>
        <TableCell scope="col" variant="header" align="right">
          Total
        </TableCell>
      </TableRow>
    </TableHead>
    <TableBody>
      {options.map((option) => (
      <TableRow key={option.numberOfInstallments}>
        <TableCell variant="header" align="left">
          {option.numberOfInstallments}x
        </TableCell>
        <TableCell align="right">
          <Price formatter={useFormattedPrice} value={option.total} />
        </TableCell>
      </TableRow>
      ))}
    </TableBody>
  </Table>
```
  </Tab>
</Tabs>

---

## Usage

### Import the component

```tsx
import { Table } from "@faststore/ui";
```

### Import styles

To apply styles, include the following in your stylesheet:

```scss
@import "@faststore/ui/src/components/molecules/Table/styles.scss";
```

For details, see [Importing FastStore UI component styles](https://developers.vtex.com/docs/guides/faststore/using-themes-importing-ui-components-styles).

---

## Examples

### Colored


<Tabs items={["Example", "Code"]} defaultIndex="0">
  <Tab>
    <OverviewSection>
      <UsageTableColored />
    </OverviewSection>
  </Tab>
  <Tab>
    ```tsx
    <Table>
      <TableHead>
        <TableRow>
          <TableCell scope="col" variant="header" align="left">
            Colored
          </TableCell>
          <TableCell scope="col" variant="header" align="right">
            Total
          </TableCell>
        </TableRow>
      </TableHead>
      <TableBody>
        {options.map((option) => (
          <TableRow key={option.numberOfInstallments}>
            <TableCell variant="header" align="left">
              {option.numberOfInstallments}x
            </TableCell>
            <TableCell align="right">
              <Price formatter={useFormattedPrice} value={option.total} />
            </TableCell>
          </TableRow>
        ))}
      </TableBody>
    </Table>
    ```
  </Tab>
</Tabs>

### With footer


<Tabs items={["Example", "Code"]} defaultIndex="0">
  <Tab>
    <OverviewSection>
      <Table>
        <TableBody>
          {options.map((option) => (
            <TableRow key={option.numberOfInstallments}>
              <TableCell variant="header" align="left">
                {option.numberOfInstallments}x
              </TableCell>
              <TableCell align="right">
                <Price formatter={useFormattedPrice} value={option.total} />
              </TableCell>
            </TableRow>
          ))}
        </TableBody>
        <TableFooter>
          <TableRow>
            <TableCell align="left">Total</TableCell>
            <TableCell align="right">
              <Price formatter={useFormattedPrice} value="604" />
            </TableCell>
          </TableRow>
        </TableFooter>
      </Table>
    </OverviewSection>
  </Tab>
  <Tab>
    ```tsx
    <Table>
      <TableBody>
        {options.map((option) => (
          <TableRow key={option.numberOfInstallments}>
            <TableCell variant="header" align="left">
              {option.numberOfInstallments}x
            </TableCell>
            <TableCell align="right">
              <Price formatter={useFormattedPrice} value={option.total} />
            </TableCell>
          </TableRow>
        ))}
      </TableBody>
      <TableFooter>
        <TableRow>
          <TableCell align="left">Total</TableCell>
          <TableCell align="right">
            <Price formatter={useFormattedPrice} value="604" />
          </TableCell>
        </TableRow>
      </TableFooter>
    </Table>
    ```
  </Tab>
</Tabs>

### Bordered

<Tabs items={["Example", "Code"]} defaultIndex="0">
  <Tab>
    <OverviewSection>
      <UsageTableBordered />
    </OverviewSection>
  </Tab>
  <Tab>
    ```tsx
    <Table variant="bordered">
      <TableHead>
        <TableRow>
          <TableCell scope="col" variant="header" align="left">
            Bordered
          </TableCell>
          <TableCell scope="col" variant="header" align="right">
            Total
          </TableCell>
        </TableRow>
      </TableHead>
      <TableBody>
        {options.map((option) => (
          <TableRow key={option.numberOfInstallments}>
            <TableCell variant="header" align="left">
              {option.numberOfInstallments}x
            </TableCell>
            <TableCell align="right">
              <Price formatter={useFormattedPrice} value={option.total} />
            </TableCell>
          </TableRow>
        ))}
      </TableBody>
    </Table>
    ```
  </Tab>
</Tabs>

### Horizontal Scroll

<Tabs items={["Example", "Code"]} defaultIndex="0">
  <Tab>
    <OverviewSection>
      <Table>
        <TableHead>
          <TableRow>
            <TableCell scope="col" variant="header" align="left">
              Installments
            </TableCell>
            <TableCell scope="col" variant="header">
              Monthly Payment
            </TableCell>
            <TableCell scope="col" variant="header">
              Min Items
            </TableCell>
            <TableCell scope="col" variant="header">
              Max Items
            </TableCell>
            <TableCell scope="col" variant="header">
              Subtotal
            </TableCell>
            <TableCell scope="col" variant="header" align="right">
              Total
            </TableCell>
          </TableRow>
        </TableHead>
        <TableBody>
          {options.map((option) => (
            <TableRow key={option.numberOfInstallments}>
              <TableCell align="left">{option.numberOfInstallments}x</TableCell>
              <TableCell>
                <Price
                  formatter={useFormattedPrice}
                  value={option.monthlyPayment}
                />
              </TableCell>
              <TableCell>{option.minItems}</TableCell>
              <TableCell>{option.maxItems}</TableCell>
              <TableCell>
                <Price
                  formatter={useFormattedPrice}
                  value={option.monthlyPayment}
                />
              </TableCell>
              <TableCell align="right">
                <Price formatter={useFormattedPrice} value={option.total} />
              </TableCell>
            </TableRow>
          ))}
        </TableBody>
      </Table>
    </OverviewSection>
  </Tab>
  <Tab>
    ```tsx
    <Table>
      <TableHead>
        <TableRow>
          <TableCell scope="col" variant="header" align="left">
            Installments
          </TableCell>
          <TableCell scope="col" variant="header">
            Monthly Payment
          </TableCell>
          <TableCell scope="col" variant="header">
            Min Items
          </TableCell>
          <TableCell scope="col" variant="header">
            Max Items
          </TableCell>
          <TableCell scope="col" variant="header">
            Subtotal
          </TableCell>
          <TableCell scope="col" variant="header" align="right">
            Total
          </TableCell>
        </TableRow>
      </TableHead>
      <TableBody>
        {options.map((option) => (
          <TableRow key={option.numberOfInstallments}>
            <TableCell align="left">{option.numberOfInstallments}x</TableCell>
            <TableCell>
              <Price
                formatter={useFormattedPrice}
                value={option.monthlyPayment}
              />
            </TableCell>
            <TableCell>{option.minItems}</TableCell>
            <TableCell>{option.maxItems}</TableCell>
            <TableCell>
              <Price
                formatter={useFormattedPrice}
                value={option.monthlyPayment}
              />
            </TableCell>
            <TableCell align="right">
              <Price formatter={useFormattedPrice} value={option.total} />
            </TableCell>
          </TableRow>
        ))}
      </TableBody>
    </Table>
    ```
  </Tab>
</Tabs>

---

## Design tokens

### Head

<TokenTable>
  <TokenRow token="--fs-table-head-weight" value="var(--fs-text-weight-bold)" />
  <TokenRow token="--fs-table-head-bkg-color" value="none" />
  <TokenRow token="--fs-table-head-padding-y" value="var(--fs-spacing-2)" />
</TokenTable>

### Cell

<TokenTable>
  <TokenRow token="--fs-table-cell-padding-x" value="var(--fs-spacing-3)" />
  <TokenRow token="--fs-table-cell-padding-y" value="var(--fs-spacing-1)" />
</TokenTable>

### Design tokens: Footer

<TokenTable>
  <TokenRow
    token="--fs-table-footer-weight"
    value="var(--fs-table-head-weight)"
  />
  <TokenRow token="--fs-table-footer-bkg-color" value="none" />
</TokenTable>

### Design tokens: Colored

<TokenTable>
  <TokenRow
    token="--fs-table-colored-bkg-color"
    value="var(--fs-color-neutral-1)"
    isColor
  />
  <TokenRow
    token="--fs-table-colored-border-radius"
    value="var(--fs-border-radius)"
  />
</TokenTable>

### Design tokens: Bordered

<TokenTable>
  <TokenRow
    token="--fs-table-bordered-border-width"
    value="var(--fs-border-width)"
  />
  <TokenRow
    token="- --fs-table-bordered-border-color"
    value="var(--fs-border-color-light)"
    isColor
  />
</TokenTable>

---

## Data attributes

You can target and override `Table` styles using the following data attributes:

`data-fs-table`

`data-fs-table-content`

`data-fs-table-head`

`data-fs-table-row`

`data-fs-table-footer`

`data-fs-table-body`

`data-fs-table-cell="head | data"`

`data-fs-table-variant="colored | bordered"`

---

## Props

All table-related components support all attributes also supported by their respective HTML tags. Besides those attributes, the following props are also supported:

### Table

<ComponentPropsSection component="Table" />

### TableBody

<ComponentPropsSection component="TableBody" />

### TableHead

<ComponentPropsSection component="TableHead" />

### TableRow

<ComponentPropsSection component="TableRow" />

### TableFooter

<ComponentPropsSection component="TableFooter" />

### TableCell

<ComponentPropsSection component="TableCell" />
