---
title: "Tag"
excerpt: Interactive badges.
keywords:
  - UI library
  - ecommerce
  - react
sidebar_custom_props:
  image: https://vtexhelp.vtexassets.com/assets/docs/src/Badge___bcedffeb307527c1606be224a7fb5ac1.png
---

The `Tag` component is an interactive `Badge`. By default, its have a close button.

## Overview

<Tabs items={['Example', 'Code']} defaultIndex="0">
  <Tab>
    <OverviewSection>
      <Tag variant="neutral" label="Tag" onClose={() => {}} />
      <Tag variant="warning" label="Warning Tag" onClose={() => {}} />
      <Tag variant="danger" label="Danger Tag" onClose={() => {}} />
      <Tag
        variant="neutral"
        size="big"
        label="Neutral & Big"
        onClose={() => {}}
      />
    </OverviewSection>
  </Tab>
  <Tab>
    ```tsx
    <Tag variant="neutral" label="Tag" onClose={() => {}} />
    <Tag variant="warning" label="Warning Tag" onClose={() => {}} />
    <Tag variant="danger" label="Danger Tag" onClose={() => {}} />
    <Tag
      variant="neutral"
      size="big"
      label="Neutral & Big"
      onClose={() => {}}
    />
    ```
  </Tab>
</Tabs>

---

## Import

Import the component from [@faststore/ui](https://developers.vtex.com/docs/guides/faststore/components-index)

```tsx
import { Tag } from '@faststore/ui'
```

### Import Styles into your FastStore project

To apply the styles of this component in your FastStore project, import the following into your stylesheet:

```scss
@import '@faststore/ui/src/components/molecules/Tag/styles.scss';
```

Follow the instructions in the [Importing FastStore UI component styles](https://developers.vtex.com/docs/guides/faststore/using-themes-importing-ui-components-styles) tutorial.

---

## Usage

<OverviewSection>
  <Tag variant="info" label="Summer Sale" onClose={() => {}} />
</OverviewSection>

```tsx
<Tag variant="info" label="Summer Sale" onClose={() => {}} />
```

---

## Props

<ComponentPropsSection component="Tag" />

---

## Design Tokens

<TokenTable>
  <TokenRow token="--fs-tag-text-color" value="var(--fs-color-text)" isColor />
  <TokenDivider />
  <TokenRow token="--fs-tag-icon-size" value="var(--fs-spacing-4)" />
  <TokenRow token="--fs-tag-icon-stroke-width" value="var(--fs-spacing-4)" />
</TokenTable>

---

## Customization

For further customization, you can use the following data attributes:

`data-fs-tag`

`data-fs-tag-label`

`data-fs-tag-icon-button`

This component inherits [Badge](https://developers.vtex.com/docs/guides/faststore/atoms-badge) CSS selectors and styles.

---

## Best Practices

### ✅ Do's

- Be cautious about the tag's positioning, writing, shape, and color to avoid any confusion with components similar in appearance.

### ❌ Don'ts

- Don't use long texts in tags.
- Don't use color alone to convey information. If color is needed to convey meaning, ensure there is a text alternative for that meaning as well.

---

## Accessibility

- Text elements must have sufficient color contrast against the background.
