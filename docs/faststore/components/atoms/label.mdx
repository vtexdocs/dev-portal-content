---
title: "Label"
excerpt: Labels describe the meaning of input fields.
sidebar_custom_props:
  image: https://vtexhelp.vtexassets.com/assets/docs/src/Label___d1037ecfcce02ddf742bc029d7ad9fea.png
components:
  - Label.tsx
---

[<i class="fa-brands fa-github"></i> Source code](https://github.com/vtex/faststore/tree/main/packages/components/src/atoms/Label)

The `Label` component provides accessible text descriptions for form controls, helping users understand the purpose and expected input of each field. Typical use cases include:

- Describing the purpose of input fields (e.g., `Email Address`, `Password`, or `Phone Number`).
- Associating descriptive text with form elements for improved accessibility.
- Providing context for user interactions in forms and surveys.

<Tabs items={["Example", "Code"]} defaultIndex="0">
  <Tab>
    <OverviewSection>
      <Label>Label</Label>
    </OverviewSection>
  </Tab>
  <Tab>
    ```tsx
    import React from "react";
    import { Label } from "@faststore/ui";

    export default function LabelBasicUsage() {
      return <Label>Label</Label>;
    }
    ```
  </Tab>
</Tabs>

---

## Usage

### Import the component

```tsx
import { Label } from "@faststore/ui";
```

### Import styles

To apply styles, include the following in your stylesheet:

```scss
@import "@faststore/ui/src/components/atoms/Label/styles.scss";
```

For details, see [Importing FastStore UI component styles](https://developers.vtex.com/docs/guides/faststore/using-themes-importing-ui-components-styles).

---

## Examples

### Label with Input

Labels are commonly used to describe input fields. Use the `htmlFor` attribute to associate the label with the [Input](https://developers.vtex.com/docs/guides/faststore/atoms-input) component.

<Tabs items={['Example', 'Code']} defaultIndex="0">
  <Tab>
    <OverviewSection>
      <Label htmlFor="email-input">Email Address</Label>
      <Input id="email-input" type="email" placeholder="Enter your email" />
    </OverviewSection>
  </Tab>
  <Tab>
    ```tsx
    import React from "react";
    import { Label } from "@faststore/ui";
    import { Input } from "@faststore/ui";

    export default function LabelWithInput() {
      return (
        <>
          <Label htmlFor="email-input">Email Address</Label>
          <Input id="email-input" type="email" placeholder="Enter your email" />
        </>
      );
    }
    ```
  </Tab>
</Tabs>

### Label with custom attributes

You can pass additional HTML attributes to customize the label behavior.

<Tabs items={['Example', 'Code']} defaultIndex="0">
  <Tab>
    <OverviewSection>
      <Label htmlFor="password-input" className="custom-label">
        Password
      </Label>
      <Input id="password-input" type="password" />
    </OverviewSection>
  </Tab>
  <Tab>
    ```tsx
    import React from "react";
    import { Label } from "@faststore/ui";
    import { Input } from "@faststore/ui";

    export default function LabelWithCustomAttributes() {
      return (
        <>
          <Label htmlFor="password-input" className="custom-label">
            Password
          </Label>
          <Input id="password-input" type="password" />
        </>
      );
    }
    ```
  </Tab>
</Tabs>

---

## Data attributes

You can target and override `Label` styles using the following data attributes:

`data-fs-label`

---

## Props

<ComponentPropsSection component="Label" />

---

## Best practices

### ✅ Dos

- Use clear and concise text that accurately describes the expected input. Be specific about what information is required.
- Ensure labels remain visible at all times and are not replaced by placeholders.
- Keep label text short and to the point, avoiding unnecessary words.

### ❌ Don'ts

- Don't use placeholder text as a substitute for labels, as placeholders disappear when users start typing.
- Don't hide labels visually while keeping them only for screen readers.
- Don't use vague or generic text like "Enter value" or "Field".
