---
title: "Price"
excerpt: Prices are used to display the price of a given product, discount and total values.
sidebar_custom_props:
  image: https://vtexhelp.vtexassets.com/assets/docs/src/Price___9acce93fd9399d7dcea4a3bb8b7de0c6.png
components:
  - Price.tsx
---

[<i class="fa-brands fa-github"></i> Source code](https://github.com/vtex/faststore/tree/main/packages/components/src/atoms/Price)

Prices are used to display the price of a given product, discount and total values.

<Tabs items={['Example', 'Code']} defaultIndex="0">
  <Tab>
    <OverviewSection>
      <Price value={62.5} variant="selling" />
      <Price value={62.5} variant="listing" />
      <Price value={62.5} variant="spot" />
      <Price value={62.5} variant="savings" />
      <Price value={62.5} variant="installment" />
    </OverviewSection>
  </Tab>
  <Tab>
    ```tsx
    <Price value={62.5} variant="selling" />
    <Price value={62.5} variant="listing" />
    <Price value={62.5} variant="spot" />
    <Price value={62.5} variant="savings" />
    <Price value={62.5} variant="installment" />
    ````
  </Tab>
</Tabs>

---

## Usage

### Import the component

```tsx
import { Price } from '@faststore/ui'
```

### Import Styles into your FastStore project

To apply the styles of this component in your FastStore project, import the following into your stylesheet:

```scss
@import '@faststore/ui/src/components/atoms/Price/styles.scss';
```

For details, see [Importing FastStore UI component styles](https://developers.vtex.com/docs/guides/faststore/using-themes-importing-ui-components-styles).

---

## Examples

### Variants

#### Installment

<Tabs items={['Example', 'Code']} defaultIndex="0">
  <Tab>
    <OverviewSection>
      <Price value={62.5} variant="installment" />
    </OverviewSection>
  </Tab>
  <Tab>
      ```tsx
      <Price value={62.5} variant="installment" />
      ```
  </Tab>
</Tabs>

#### Listing

<Tabs items={['Example', 'Code']} defaultIndex="0">
  <Tab>
    <OverviewSection>
      <Price value={62.5} variant="listing" />
    </OverviewSection>
  </Tab>
  <Tab>
      ```tsx
      <Price value={62.5} variant="listing" />
      ```
  </Tab>
</Tabs>

#### Savings

<Tabs items={['Example', 'Code']} defaultIndex="0">
  <Tab>
    <OverviewSection>
      <Price value={62.5} variant="savings" />
    </OverviewSection>
  </Tab>
  <Tab>
      ```tsx
      <Price value={62.5} variant="savings" />
      ```
  </Tab>
</Tabs>

#### Selling

<Tabs items={['Example', 'Code']} defaultIndex="0">
  <Tab>
    <OverviewSection>
      <Price value={62.5} variant="selling" />
    </OverviewSection>
  </Tab>
  <Tab>
      ```tsx
      <Price value={62.5} variant="selling" />
      ```
  </Tab>
</Tabs>

#### Spot

<Tabs items={['Example', 'Code']} defaultIndex="0">
  <Tab>
    <OverviewSection>
      <Price value={62.5} variant="spot" />
    </OverviewSection>
  </Tab>
  <Tab>
      ```tsx
      <Price value={62.5} variant="spot" />
      ```
  </Tab>
</Tabs>

### INTL Formatted to parts

<Tabs items={['Example', 'Code']} defaultIndex="0">
  <Tab>
    <OverviewSection>
      <Price
        formatter={function useIntlPartsFormatter(price) {
            return useMemo(() => {
              return new Intl.NumberFormat('en-US', {
                style: 'currency',
                currency: 'USD',
              })
                .formatToParts(price)
              .map((part) => {
                const props = {
                  [`data-fs-price-${part.type}`]: true,
                }

                if (part.type === 'integer') {
                  props.style = { fontWeight: 700 }
                }

                return (
                  <span key={part.type} {...props}>
                    {part.value}
                  </span>
                )
              })
          })
        }}
        value={62.5}
        variant="selling"

    />

    </OverviewSection>
  </Tab>
  <Tab>
    ```tsx
    <Price
      formatter={function useIntlPartsFormatter(price) {
        return useMemo(() => {
          return new Intl.NumberFormat('en-US', {
            style: 'currency',
            currency: 'USD',
          })
            .formatToParts(price)
            .map((part) => {
              const props = {
                [`data-fs-price-${part.type}`]: true,
              }

              if (part.type === 'integer') {
                props.style = { fontWeight: 700 }
              }

              return (
                <span key={part.type} {...props}>
                  {part.value}
                </span>
              )
            })
        })
      }}
      value={62.5}
      variant="selling"
    />
    ```
  </Tab>
</Tabs>


### INTL Formatted

<Tabs items={['Example', 'Code']} defaultIndex="0">
  <Tab>
    <OverviewSection>
      <Price
        formatter={function useIntlFormatter(price) {
          return useMemo(() => {
            const formattedPrice = new Intl.NumberFormat('en-GB', {
              style: 'currency',
              currency: 'EUR',
            }).format(price)

            return formattedPrice
          })
        }}
        value={62.5}
        variant="savings"

    />
    </OverviewSection>
  </Tab>
  <Tab>
    ```tsx
    <Price
      formatter={function useIntlFormatter(price) {
        return useMemo(() => {
          const formattedPrice = new Intl.NumberFormat('en-GB', {
            style: 'currency',
            currency: 'EUR',
          }).format(price)

          return formattedPrice
        })
      }}
      value={62.5}
      variant="savings"
    />
    ```
  </Tab>
</Tabs>

### Custom

<Tabs items={['Example', 'Code']} defaultIndex="0">
  <Tab>
    <OverviewSection>
      <Price
        formatter={function customFormatter(price) {
          const unformattedPrice = `${price}`
          const formattedPrice = `${unformattedPrice.replace('.', ',')} reais`
          return formattedPrice
        }}
        value={62.5}
        variant="spot"
      />
    </OverviewSection>
  </Tab>
  <Tab>
    ```tsx
    <Price
      formatter={function customFormatter(price) {
        const unformattedPrice = `${price}`
        const formattedPrice = `${unformattedPrice.replace('.', ',')} reais`
        return formattedPrice
      }}
      value={62.5}
      variant="spot"
    />
    ```
  </Tab>
</Tabs>

---

## Formatter function example

```tsx
function customFormatter(price: number) {
  const unformattedPrice = `${price}`
  const formattedPrice = `${unformattedPrice.replace('.', ',')} reais`

  return formattedPrice
}
```

---

## Design tokens

<TokenTable>
  <TokenRow
    token="--fs-price-listing-color"
    value="var(--fs-color-text-light)"
    isColor
  />
  <TokenRow token="--fs-price-listing-text-decoration" value="line-through" />
  <TokenRow
    token="--fs-price-spot-font-weight"
    value="var(--fs-text-weight-bold)"
  />
  <TokenRow
    token="--fs-price-spot-color"
    value="var(--fs-color-text)"
    isColor
  />
</TokenTable>

---

## Data attributes

You can target and override `Price` styles using the following data attributes:

`data-fs-price`

`data-fs-price-variant="selling| 'listing' | 'spot' | 'savings'| 'installment"`

---

## Props

<ComponentPropsSection component="Price" />
