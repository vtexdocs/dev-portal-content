---
title: "Overlay"
excerpt: Overlays provide contextual information on top of the current layer of the interface.
sidebar_custom_props:
  image: https://vtexhelp.vtexassets.com/assets/docs/src/Overlay___5ae3e4dc6b4b413cf33295d52a64fac0.png
components:
  - Overlay.tsx
---

[<i class="fa-brands fa-github"></i> Source code](https://github.com/vtex/faststore/tree/main/packages/components/src/atoms/Overlay)

Overlays provide contextual information on top of the current layer of the interface. They can be interruptive like modals or augmenting like popovers and tooltips.

<Tabs items={['Example', 'Code']} defaultIndex="0">
  <Tab>
    <OverviewSection>
      <div
        style={{
          position: 'relative',
          height: '8rem',
          width: '100%',
          textAlign: 'center',
        }}
      >
        A content outside me
        <Overlay style={{ position: 'absolute' }} />
      </div>
    </OverviewSection>
  </Tab>
  <Tab>

```tsx
function Component() {
  const styles = {
    container: {
      position: 'relative',
      height: '8rem',
    },
    overlay: {
      position: 'absolute',
    },
  }
  return (
    <div style={styles.container}>
      A content outside me
      <Overlay style={styles.overlay} />
    </div>
  )
}
```

</Tab>
</Tabs>

---

## Usage

### Import the component

```tsx
import { Overlay } from '@faststore/ui'
```

### Import styles

To apply styles, include the following in your stylesheet:

```scss
@import '@faststore/ui/src/components/atoms/Overlay/styles.scss';
```

For details, see [Importing FastStore UI component styles](https://developers.vtex.com/docs/guides/faststore/using-themes-importing-ui-components-styles).

---

## Design tokens

<TokenTable>
  <TokenRow token="--fs-overlay-bkg-color" value="rgb(0 0 0 / 20%)" isColor />
</TokenTable>

---

## Data attributes

You can target and override `Overlay` styles using the following data attributes:

`data-fs-overlay`

---

## Props

<ComponentPropsSection component="Overlay" />
