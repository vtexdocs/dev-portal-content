---
title: "Icon"
excerpt: Icons are simplified graphics that represent actions, statuses, and navigation elements, helping users navigate the UI.
sidebar_custom_props:
  image: https://vtexhelp.vtexassets.com/assets/docs/src/Icon___9437da8db13517090534a38f4870d702.png
components:
  - Icon.tsx
---

[<i class="fa-brands fa-github"></i> Source code](https://github.com/vtex/faststore/tree/main/packages/components/src/atoms/Icon)

The `Icon` component provides SVG-based icons from the FastStore [Iconography library](https://developers.vtex.com/docs/guides/faststore/reference-icons), built on top of [Phosphor Icons](https://phosphoricons.com). 

Icons are used across the interface to improve clarity and usability in different contexts, such as:

- **Navigation:** Provide cues that help users move through menus, pages, and the homepage.
- **User actions:** Represent tasks like adding items to the cart, sharing content, or saving to favorites.
- **System feedback:** Indicate statuses such as information, success, warning, or error. 
- **Commerce elements:** Represent the cart, account, or checkout.
- **Form inputs:** Improve field usability with search, clearing fields, toggling password visibility, or copying content. 
- **Menus and lists:** Indicate dropdowns, pagination, or breadcrumb trails.

>ℹ️ Browse the full set of icons in [Iconography](https://developers.vtex.com/docs/guides/faststore/reference-icons).

<Tabs items={['Example', 'Code']} defaultIndex="0">
  <Tab>
    <OverviewSection>
      <Icon name="ShoppingCart" weight="thin" width={32} height={32} />
      <Icon name="ShoppingCart" weight="light" />
      <Icon name="ShoppingCart" weight="regular" width={16} height={16} />
      <Icon name="ShoppingCart" weight="bold" width={20} height={20} />
    </OverviewSection>
  </Tab>
  <Tab>

  ```tsx
  import React from 'react'
  import { Icon } from '@faststore/ui'

  export default function IconOverviewExamples() {
    return (
      <div>
        <Icon name="ShoppingCart" weight="thin" width={32} height={32} />
        <Icon name="ShoppingCart" weight="light" />
        <Icon name="ShoppingCart" weight="regular" width={16} height={16} />
        <Icon name="ShoppingCart" weight="bold" width={20} height={20} />
      </div>
    )
  }
  ```
  </Tab>
</Tabs>

> ⚠️ The default values for `width` and `height` are `24`.

---

## Usage

### Import the component

```tsx
import { Icon } from "@faststore/ui";
```

### Import styles

To apply styles, include the following in your stylesheet:

```scss
@import "@faststore/ui/src/components/atoms/Icon/styles.scss";
```

For details, see [Importing FastStore UI component styles](https://developers.vtex.com/docs/guides/faststore/using-themes-importing-ui-components-styles).

---

## Examples

### Weight

Use the `weight` prop to control the visual stroke weight of the icon. Available options are `thin`, `light`, `regular` (default), and `bold`.

#### Bold

<Tabs items={['Example', 'Code']} defaultIndex="0">
  <Tab>
    <OverviewSection>
      <Icon name="ShoppingCart" weight="bold" />
    </OverviewSection>
  </Tab>
  <Tab>

  ```tsx
  import React from 'react'
  import { Icon } from '@faststore/ui'

  export default function IconWeightBold() {
    return <Icon name="ShoppingCart" weight="bold" />
  }
  ```
  </Tab>
</Tabs>

#### Light

<Tabs items={['Example', 'Code']} defaultIndex="0">
  <Tab>
    <OverviewSection>
      <Icon name="ShoppingCart" weight="light" />
    </OverviewSection>
  </Tab>
  <Tab>

  ```tsx
  import React from 'react'
  import { Icon } from '@faststore/ui'

  export default function IconWeightLight() {
    return <Icon name="ShoppingCart" weight="light" />
  }
  ```
  </Tab>
</Tabs>

#### Regular (default)

<Tabs items={['Example', 'Code']} defaultIndex="0">
  <Tab>
    <OverviewSection>
      <Icon name="ShoppingCart" weight="regular" />
    </OverviewSection>
  </Tab>
  <Tab>

  ```tsx
  import React from 'react'
  import { Icon } from '@faststore/ui'

  export default function IconWeightRegular() {
    return <Icon name="ShoppingCart" weight="regular" />
  }
  ```
  </Tab>
</Tabs>

#### Thin

<Tabs items={['Example', 'Code']} defaultIndex="0">
  <Tab>
    <OverviewSection>
      <Icon name="ShoppingCart" weight="thin" />
    </OverviewSection>
  </Tab>
  <Tab>

  ```tsx
  import React from 'react'
  import { Icon } from '@faststore/ui'

  export default function IconWeightThin() {
    return <Icon name="ShoppingCart" weight="thin" />
  }
  ```
  </Tab>
</Tabs>

### Size

Control icon size with the `width` and `height` props. If omitted, both default to `24` (medium size).

#### Small

<Tabs items={['Example', 'Code']} defaultIndex="0">
  <Tab>
    <OverviewSection>
      <Icon name="ShoppingCart" width={16} height={16} />
    </OverviewSection>
  </Tab>
  <Tab>

  ```tsx
  import React from 'react'
  import { Icon } from '@faststore/ui'

  export default function IconSize16() {
    return <Icon name="ShoppingCart" width={16} height={16} />
  }
  ```
  </Tab>
</Tabs>

#### Medium (default)

<Tabs items={['Example', 'Code']} defaultIndex="0">
  <Tab>
    <OverviewSection>
      <Icon name="ShoppingCart" />
    </OverviewSection>
  </Tab>
  <Tab>

  ```tsx
  import React from 'react'
  import { Icon } from '@faststore/ui'

  export default function IconSizeDefault() {
    return <Icon name="ShoppingCart" />
  }
  ```
  </Tab>
</Tabs>

#### Large

<Tabs items={['Example', 'Code']} defaultIndex="0">
  <Tab>
    <OverviewSection>
      <Icon name="ShoppingCart" width={32} height={32} />
    </OverviewSection>
  </Tab>
  <Tab>

  ```tsx
  import React from 'react'
  import { Icon } from '@faststore/ui'

  export default function IconSize32() {
    return <Icon name="ShoppingCart" width={32} height={32} />
  }
  ```
  </Tab>
</Tabs>
---

## Design tokens

The `Icon` component does not expose specific design tokens. Customize its appearance using:

- The [`weight`](#weight) prop (`thin`, `light`, `regular`, `bold`).
- CSS selectors targeting the `[data-fs-icon]` data attribute for layout adjustments. The default theme applies only structural styles to this selector.

---

## Data attributes

You can target and override `Icon` styles using the following data attributes:

`data-fs-icon`

---

## Props

<ComponentPropsSection component="Icon" />

---

## Best practices

### ✅ Do's
- Ensure icons are visually consistent in weight and size within the same context.
- Provide sufficient contrast against varied backgrounds.
- Pair icons with text when meaning may be ambiguous.

### ❌ Don'ts

- Don't rely solely on color or the icon graphic to convey critical meaning.
- Don't mix different icon weights within the same control or pattern.
