---
title: "SROnly"
excerpt: Hides an element visually. Improve accessibility by providing text to screen readers when applied.
---

[<i class="fa-brands fa-github"></i> Source code](https://github.com/vtex/faststore/tree/main/packages/components/src/atoms/SROnly)

The `SROnly` component hides content visually while keeping it available to screen readers. Use it to provide additional context or accessible labels without impacting the visual design.

<Tabs items={['Example', 'Code']} defaultIndex="0">
  <Tab>
    <OverviewSection>
      <SROnly text="visually hidden text" />
    </OverviewSection>
  </Tab>
  <Tab>
    ```tsx
    <SROnly text="visually hidden text" />
    ```
  </Tab>
</Tabs>

## Usage

### Import the component

```tsx
import { SROnly } from '@faststore/ui'
```

### Import Styles into your FastStore project

To apply the styles of this component in your FastStore project, import the following into your stylesheet:

```scss
@import '@faststore/ui/src/components/atoms/SROnly/styles.scss';
```

For details, see [Importing FastStore UI component styles](https://developers.vtex.com/docs/guides/faststore/using-themes-importing-ui-components-styles).

---

## Data attributes

You can target and override `SROnly` styles using the following data attributes:

`data-fs-sr-only`

>⚠️ Be cautious when modifying this style, as changes may break its functionality.

---

## Props

<PropsSection propsList={propsSROnly} />
