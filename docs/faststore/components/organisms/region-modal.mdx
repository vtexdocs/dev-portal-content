---
title: "RegionModal"
excerpt: This component is a modal with some region configurations.
sidebar_custom_props:
  image: https://vtexhelp.vtexassets.com/assets/docs/src/Button___6d6b16886d5d7e2a05dba8c45c075796.png
components:
  - RegionModal.tsx
---

The `RegionModal` is part of the [Regionalization](https://developers.vtex.com/docs/guides/faststore/feature-regionalization) feature and provides a modal interface for selecting and configuring region-specific options. It extends the functionality of the [Modal](https://developers.vtex.com/docs/guides/faststore/molecules-modal) component, making it adaptable for region-related settings within your FastStore application.

## Import

Import the component from [@faststore/ui](https://developers.vtex.com/docs/guides/faststore/components-index)

```tsx
import { RegionModal } from '@faststore/ui'
```

### Import styles into your FastStore project

To apply the styles of this component in your FastStore project, add the following line to your stylesheet:

```scss
@import '@faststore/ui/src/components/organisms/RegionModal/styles.scss';
```

Follow the instructions in the [Importing FastStore UI component styles](https://developers.vtex.com/docs/guides/faststore/using-themes-importing-ui-components-styles) guide.

---

## Usage

<Tabs items={['Example', 'Code']} defaultIndex="0">
  <Tab>
    <OverviewSection>
      <UIProvider>
        <RegionModalUsage />
      </UIProvider>
    </OverviewSection>

  </Tab>
  <Tab>
    ```tsx
    const { modal, openModal } = useUI()
    const [input, setInput] = useState('')
    return (
      <>
        <Button variant="primary" onClick={() => openModal()}>
          Open Region Modal
        </Button>
        {modal && (
          <RegionModal
            fadeOutOnSubmit={true}
            inputValue={input}
            onInput={(e) => setInput(e.currentTarget.value)}
          />
        )}
      </>
    )
    ```

  </Tab>
</Tabs>

---

## Props

<ComponentPropsSection component="RegionModal" />

---

## Design Tokens

<TokenTable>
  <TokenRow
    token="--fs-region-modal-margin-bottom"
    value="var(--fs-spacing-6)"
  />
  <TokenDivider />
  <TokenRow token="--fs-region-modal-link-padding" value="0" />
  <TokenRow
    token="--fs-region-modal-link-column-gap"
    value="var(--fs-spacing-0)"
  />
  <TokenRow
    token="--fs-region-modal-link-color"
    value="var(--fs-color-link)"
    isColor
  />
</TokenTable>

---

## Customization

For further customization, you can use the following data attributes:

`data-fs-region-modal`

`data-fs-region-modal-input`

`data-fs-region-modal-link`
